centroid = function() {
	print("this is a function");
}

const .print = function(x) {
	str(x);
	x;
}

const align = function(centroid = "da:0.3") {
	list(
		centroid = centroid
	)
	|> .print()
	;
}

# da:0.3
align();
# R function
align(centroid);

# print R function
print(centroid);